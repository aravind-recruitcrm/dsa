1.https://github.com/AravindBusha/dsa/blob/main/Greedy/Activity%20Selection%20(gfg)

2. https://www.youtube.com/watch?v=II6ziNnub1Q&list=PLgUwDviBIf0pmWCl2nepwGDO05a0-7EfJ

 3.class meetingComparator implements Comparator<meeting>
    {
    @Override
    public int compare (meeting o1, meeting o2)
    {
    if (01.end < 02.end)
    return -1;
    else if (01.end > 02.end)
    return 1;
    else if(o1.pos < 02.pos)
    return -1;
    return 1;
    }
    }
  
 4. we need to use the string1.compareTo(string2) in the comparator
    example: 
      PriorityQueue<Pair> pq = new PriorityQueue<>((Pair a, Pair b)-> {
            if(a.freq==b.freq) return (a.word).compareTo(b.word);
            return b.freq-a.freq;
        });
